# test-support

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build test-support` to build the library.

## Running unit tests

Run `nx test test-support` to execute the unit tests via [Jest](https://jestjs.io).
